import React, { Component } from 'react';
import {
   StyleSheet,
   View,
} from 'react-native';

import MModuleTabbar from './MModuleTabbar';

export default class MModuleTabbarController extends Component {
   constructor(props) {
   }

   render() {
      return (
         // here
      );
   }
}

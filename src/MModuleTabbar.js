import React, { Component } from 'react';
import {
   Image,
   Text,
   TouchableOpacity,
   View,
} from 'react-native';

//tabbar default height: 49

export default class MModuleTabbar extends Component {
   render() {
      return(
         //
      );
   }
}

var styles = StyleSheet.create({
   container: {
      height: 49, // iOS Tabbar's default height since iOS8.
   },
});
